# CommitNoteAI Context

## 项目定位
- CommitNoteAI 是 IntelliJ Platform / Android Studio 插件。
- 插件根据 Commit 工具窗口中已经勾选的变更生成中文 Git 提交记录。
- 生成结果必须以真实 diff、changed symbols、文件路径、代码片段和当前提交草稿为依据。
- 本文件只提供项目术语、模块边界和提交偏好，不能替代真实 diff。

## 可靠性规则
- 只描述已勾选 diff 中真实出现的改动。
- 只使用 diff、changed symbols、文件名、类名、方法名、字段名、配置名或本文件中能找到证据的说法。
- 如果只看到局部方法或局部文件改动，只总结局部事实，不推断完整业务流程。
- 如果 diff 只改文档、测试或配置，不要写成运行时功能变化。
- 如果 diff 只改 UI 文案、按钮、布局属性或资源引用，不要推断页面行为已经变化。
- 如果 diff 只新增接口、模型或工具函数，不要写成已经完成端到端接入。
- 不写“优化体验”“完善逻辑”“提升稳定性”“修复问题”等无证据泛话。
- 不把新增文件数量多的提交自动写成“完整支持”或“全面接入”，除非 diff 明确包含完整链路。
- 正文中提到的方法名、字段名、类名、文件名必须能在 diff、changed symbols 或本文件中找到。

## 模块与 scope
- `ai`: AI 请求、响应解析、JSON 契约、模型列表、API Base URL、reasoning、temperature、错误处理。
- `vcs`: Commit 工具窗口入口、生成按钮、CheckinHandler、提交框回填、打字机效果、VCS 变更读取入口。
- `util`: diff 收集、片段提取、符号提取、prompt 构建、事实校验、提交信息清洗、项目上下文读取。
- `settings`: 设置页、API Key、PasswordSafe、模型配置、输出样式、自定义偏好、创建上下文模板。
- `model`: 提交生成相关数据结构、wire payload、prompt payload、change snapshot。
- `platform`: IntelliJ Platform 兼容桥接，例如 Java/Kotlin 跨语言访问或平台 API 包装。
- `plugin`: `plugin.xml`、插件声明、action 注册、版本、兼容构建号、图标和插件元数据。
- `gradle`: Gradle、wrapper、构建配置、插件打包、依赖版本。
- `docs`: README、AGENTS、CLAUDE、CommitNoteAI 上下文等文档。
- `test`: 单元测试、测试样例、断言、测试覆盖。
- 多个模块同时变化时，优先选择最能表达主要行为的 scope；无法判断时可以省略 scope。

## type 选择
- `feat`: 新增用户可见能力、插件入口、配置项、生成流程能力或完整可用的新模块。
- `fix`: 修复错误行为、解析失败、回填异常、设置校验、事实校验误判或兼容问题。
- `refactor`: 调整结构、抽取工具、移动职责、删除冗余实现，不改变外部行为。
- `chore`: 构建、版本、依赖、Gradle、元数据、非用户可见维护改动。
- `docs`: 只改文档、说明、上下文模板或注释性内容。
- `test`: 只新增或调整测试。
- `style`: 只改格式、排版、命名风格或无行为差异的代码样式。

## 正文偏好
- 使用中文短句清单，每条只讲一个具体事实。
- 每条以明确动作开头，例如：新增、调整、扩展、移除、修复、更新、重构。
- 优先写具体的类名、方法名、字段名、配置项、文件名和资源名。
- 优先覆盖 addedSymbols、removedSymbols、关键文件和事实校验相关改动。
- 避免一条里堆多个“并且 / 同时 / 以及”；过长时拆成两条。
- 不使用 Markdown 前缀、编号或代码块；bodyLines 只返回正文文本。
- 标题不要使用“完整”“全面”“彻底”“大幅”等过满词，除非 diff 明确支持。

## 高风险表述黑名单
- 没有直接证据时，不写“页面流程”“返回键处理”“状态更新”“生命周期管理”。
- 没有直接证据时，不写“完整支持”“全面接入”“端到端打通”“全链路优化”。
- 没有直接证据时，不写“提升用户体验”“增强稳定性”“优化性能”“改善交互”。
- 没有直接证据时，不写“修复崩溃”“解决异常”“避免重复请求”“防止状态错乱”。
- 没有直接证据时，不写“兼容所有模型”“支持所有 OpenAI 接口”“自动识别全部场景”。

## 示例与反例

### 示例一：只改 prompt 规则
好：
- `fix(util): 收紧提交记录生成的事实约束`
- `新增 changed symbols 证据规则`
- `限制正文描述未出现在 diff 中的页面流程`

坏：
- `feat(ai): 全面提升提交记录智能生成能力`
- `优化用户体验并提升提交质量`

原因：prompt 规则变化不能直接宣称完整能力升级，正文要写具体规则。

### 示例二：只改设置页配置
好：
- `feat(settings): 新增输出样式配置`
- `在 CommitNoteConfigurable 中加入输出样式选择框`
- `保存 outputStyle 到 CommitNoteSettings`

坏：
- `feat(ai): 完整支持多种提交生成模式`
- `优化生成流程和模型表现`

原因：设置页改动只证明配置入口变化，不证明 AI 生成效果全面提升。

### 示例三：只改测试
好：
- `test(util): 补充项目上下文读取测试`
- `覆盖 CommitNoteAI.md 缺失时返回空字符串`
- `验证超长上下文会被裁剪`

坏：
- `fix(util): 修复项目上下文读取异常`
- `提升上下文加载稳定性`

原因：只改测试时通常使用 `test`，不要宣称生产代码修复。
